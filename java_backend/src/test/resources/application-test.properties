local.management.port=0
spring.jackson.parser.include-source-in-location=true
spring.datasource.jdbcUrl=jdbc:h2:mem:testdb;user=sa;MODE=PostgreSQL;
spring.datasource-demo.jdbcUrl=jdbc:h2:mem:testdb;user=sa;MODE=PostgreSQL;
spring.datasource-extra.jdbcUrl=jdbc:h2:mem:testdb;user=sa;MODE=PostgreSQL;
periodic-background-task.schedule=0 0 0 25 12 ?
