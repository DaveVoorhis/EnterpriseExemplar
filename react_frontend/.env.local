VITE_API_BASE=http://localhost:8080/api
VITE_SSO_AUTHORITY=http://localhost:5556
# Optional: set a dev token for local runs.
# If not provided, the app will read from localStorage('authToken').
VITE_AUTH_TOKEN=xxxx.yyyy.zzzz
